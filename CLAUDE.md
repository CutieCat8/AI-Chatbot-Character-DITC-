# DITC CAT

ผู้ช่วยตอบคำถามด้วยเสียงสำหรับศูนย์ DITC คณะ CAMT มหาวิทยาลัยเชียงใหม่
ตัวละครเป็นแมว ผู้ใช้ยืนหน้าตู้แล้วพูดคุยด้วยเสียง ไม่มีคีย์บอร์ด

## ฮาร์ดแวร์จริง (สำคัญมาก — ต้องคิดถึงทุกครั้ง)

- ตู้คือ Samsung Galaxy Tab S10 FE+ Wi-Fi รัน Android
- **ไมค์กับลำโพงอยู่ในเครื่องเดียวกัน ไม่มี AEC ฮาร์ดแวร์**
  จึงใช้ half-duplex (ปิดไมค์ตอนแมวพูด) ห้ามเปลี่ยนกลับเป็น full-duplex
  โดยไม่คุยก่อน
- แท็บเล็ตเปิดแค่ frontend ผ่าน Chrome เท่านั้น
  **รัน Python / Docker / โมเดล embedding บนแท็บเล็ตไม่ได้**
- backend รันบนเครื่องแยกในวง LAN เดียวกัน
- Chrome Android บล็อก getUserMedia บน origin ที่ไม่ใช่ HTTPS
  ต้องจัดการเรื่อง cert หรือ flag ทุกครั้งที่ทดสอบบนแท็บเล็ต

## Stack

- monorepo + Docker Compose
- backend: FastAPI, Postgres + pgvector
- frontend-admin: React + Tailwind (แดชบอร์ดผู้ดูแล)
- frontend-character: Vite + React + TS (หน้าจอแมว พอร์ต 5174)
- embedding: multilingual-e5-large รันเอง dim=1024
- LLM สำหรับ Chat Demo ในแอดมิน: DeepSeek
- เสียงหน้าตู้: Gemini Live (gemini-3.1-flash-live-preview)
  speech-to-speech เรียก retrieval.py ผ่าน function calling
  จำกัด language_codes เป็น ["th-TH", "en-US"] เสมอ (ไม่ปล่อย
  auto-detect เปิดกว้างทุกภาษา เคยเจอบั๊กเดาเป็นอินโดนีเซีย)
- ทั้ง Chat Demo และตู้ ใช้ retrieval.py ตัวเดียวกัน

## กติกาการทำงาน

1. **ห้ามรายงานว่า "ทดสอบแล้ว" ถ้าไม่ได้ทดสอบด้วยของจริง**
   ถ้าใช้ข้อความแทนเสียง ใช้ TTS แทนเสียงคน หรือใช้ mock
   ต้องเขียนชัดว่าใช้อะไร และบอกว่ายังไม่ได้พิสูจน์อะไร
2. **วัดสิ่งที่สำคัญ ไม่ใช่สิ่งที่วัดง่าย**
   ตัวชี้วัดคือคำตอบถูกไหม ไม่ใช่ transcript ตรงตัวอักษรไหม
3. **บอกล่วงหน้าถ้าต้อง reindex** ทุกการเปลี่ยน embedding หรือ chunking
   ต้องบอกว่ากระทบ index เดิมไหม ใช้เวลาเท่าไหร่
4. **ห้าม map คำที่ฟังผิดแบบมั่ว** เช่น Disney → DITC เพราะจะพัง
   เมื่อมีคนถามเรื่อง Disney จริง
5. **ตัดสินใจที่มีผลระยะยาวให้ถามก่อน** อย่าทำแล้วค่อยบอก
6. เขียน ADR ลง docs/adr/ ทุกครั้งที่เลือกทางใดทางหนึ่งจากหลายทาง
7. เมื่อทำงานเสร็จทุกครั้ง ให้เรียก reviewer subagent ตรวจก่อนรายงานผู้ใช้

## สิ่งที่ตัดสินใจไปแล้ว ห้ามรื้อโดยไม่ถาม

- ไม่ใช้ Porcupine เพราะไม่รองรับภาษาไทย ใช้ VAD แทน
- ไม่ใช้ OpenAI embedding เพราะ e5-large ทำภาษาไทยดีกว่าและฟรี
  (ผลเทียบอยู่ใน docs/adr/embedding-model.md)
- ใช้ half-duplex เพราะฮาร์ดแวร์ไม่มี AEC

## บริบทสัญญา

- งานจ้างเหมาจาก DITC วงเงิน 45,000 บาท ระยะเวลา 40 วัน
- มีค่าปรับรายวัน 0.10% ของงานที่ยังไม่ส่งมอบ
- รับประกันความชำรุด 1 ปีหลังส่งมอบ
- ลิขสิทธิ์และกรรมสิทธิ์ตกเป็นของ DITC
- ติดตั้งบนเครื่องแม่ข่ายที่หน่วยงานกำหนด ไม่ใช่เครื่อง dev
- ค่า API รายเดือนหลังส่งมอบ ต้องระบุผู้รับผิดชอบก่อนส่งงาน

## ของที่ต้องส่งมอบ (นอกจากโค้ด)

- คู่มือการใช้งานสำหรับผู้ดูแลระบบ
- เอกสารสถาปัตยกรรมระบบสำหรับดูแลรักษาต่อ
- ไฟล์ต้นฉบับ Character + แอนิเมชัน (SVG placeholder ส่งไม่ได้)
- Source code ทั้งหมดและไฟล์ที่จำเป็นสำหรับติดตั้ง

## ฟีเจอร์ในสโคปที่ยังไม่ได้ทำ — ห้ามลืม

- ระบบฟีดแบค: ผู้ใช้พูดว่า "ฟีดแบค" เมื่อไหร่ก็ได้เพื่อฝากความเห็น
  AI สรุปเป็นหมวดหมู่ + โทนบวก/กลาง/ลบ
- ~~Idle mode: วนแสดงข่าวของศูนย์ตอนไม่มีคนใช้~~ **ตัดออกแล้ว 2026-09-07** (เจ้าของงานตัดสินใจ
  ไม่ทำ — ดูเหตุผลเต็มที่หัวข้อ "ข้อกำหนดที่ห้ามละเมิด" ด้านล่าง เรื่องสถานะแมว รอยืนยันกับที่ปรึกษา
  โครงงานอีกที)
- ตอบด้วยเสียง + ภาพประกอบ ไม่ใช่เสียงอย่างเดียว
- ดึงข้อมูลสดจากเว็บ DITC เมื่อไม่พบในฐานความรู้
- แสดงอารมณ์ประกอบบทสนทนา
- Wake word — **ยังอยู่ในสโคป ไม่ได้ตัดออก** (ต่างจาก "Idle mode" ด้านบนที่ตัดจริง) วลีที่เขียนไว้เดิม
  "สวัสดีดิตซีแคท" **ผิด แก้แล้ว 2026-09-11** เริ่มเทรนโมเดล wake-word จริงแล้ว (ดู
  `docs/superpowers/specs/2026-09-08-wake-word-design.md` และ handoff doc คู่กัน) เจ้าของงานยืนยันวลี
  positive จริงคือ 6 รายการใน `generate_dataset.py` POSITIVE_TEXTS (แหล่งความจริง อย่า list ซ้ำที่นี่
  เพราะจะหลุดตามกันได้ — ไปอ่านไฟล์นั้นตรงๆ) **ไม่มีคำว่า "แคท"** ในรายการไหนเลย สโคป (Overview) ยัง
  บังคับว่าต้องทดสอบกับผู้ใช้จริงหลายคนก่อนเปิดใช้งานจริง — มี dev-only implementation แล้ว
  (`frontend-character/src/hooks/useWakeWord.ts`, `?wakeword=1` ใน `App.tsx`) แต่โมเดลยัง **ไม่ผ่าน
  production gate** ห้ามเปิดเป็น default ของ kiosk งานทั้งหมดยัง uncommitted อยู่ใน branch
  `debug/voice-click-noise-investigation`

## ข้อกำหนดที่ห้ามละเมิด

- **รองรับสองภาษา ไทยและอังกฤษ ตรวจจับอัตโนมัติ**
  ห้ามล็อกภาษาเดียว (แก้แล้ว 2026-09-06: เปลี่ยนจากล็อกเดี่ยว th-TH
  เป็น language_codes=["th-TH", "en-US"] — ยังกันบั๊ก auto-detect
  หลุดไปภาษาอื่นได้เหมือนเดิม แค่ไม่ได้ล็อกภาษาเดียวอีกต่อไป)
- ไม่มี touchscreen เพราะมีกระจกครอบ ทุกฟังก์ชันสั่งด้วยเสียง
- **4 สถานะแมวจริงตาม TOR: Idle / Transition / Sleep / Wake** (แก้แล้ว 2026-09-07 — เดิมมี 5
  สถานะรวม "Web" ด้วย แต่ตัด Web ออกแล้ว ดูเหตุผลด้านล่าง)

  **ทำไมตัด "Web" ออก:** ตอนแรกทีมตีความ "Web" ผิดว่าเป็นสถานะ "กำลังค้นข้อมูล" (retrieval/thinking)
  แต่ความหมายจริงใน TOR คือ **โหมดแสดงข่าวเลื่อนวนตอนไม่มีคนใช้งาน** — ออกแบบไว้ตอนคิดว่าจอแท็บเล็ต
  จะสัมผัสได้ (ให้คนเปิดดูเว็บ/ข่าวระหว่างไม่มีคนคุย) แต่ตัวเครื่องจริงมีกระจกครอบ ไม่รองรับ
  touchscreen เลย ทุกอย่างสั่งด้วยเสียงล้วน หน้าจอเหลือแค่หน้าแมวอย่างเดียว — โหมดแสดงข่าวจึงไม่มี
  ประโยชน์กับ use case ที่เหลือ เจ้าของงานเลยตัดสินใจตัดออก **(รอยืนยันกับที่ปรึกษาโครงงานอีกที)**

  **ตาราง map CatState (4 ค่า) → CatFace render-state (7 ค่า จาก Figma, ดู
  `frontend-character/src/components/character/CatFace.tsx`):**

  | CatState (TOR) | CatFace render-state | หมายเหตุ |
  |---|---|---|
  | `idle` | `idle` | |
  | `transition` | `idle` | ไม่มี render แยกของตัวเอง |
  | `sleep` | `sleeping` | |
  | `wake` | `listening` / `thinking` / `speaking` | `listening`=กำลังฟังผู้ใช้, `thinking`=หยุดพูดแล้วรอ Gemini ตอบ (`isThinking`), `speaking`=แมวพูดตอบ (`botSpeaking`) — ทั้งหมดต่อกับไมค์จริงแล้ว (2026-09-07/08) |
  | *(offTopic flag ชนะทุก state)* | `angry` | ต่อกับ backend จริงแล้ว (2026-09-08) — Gemini เรียก tool `flag_off_topic` เอง (ทาง ข ไม่เดาจาก keyword) ส่ง `{"type":"off_topic"}` ผ่าน WS หมดอายุเองพร้อม `wake→transition→idle` ปกติ **และ**เคลียร์ทันทีถ้า WS หลุดกะทันหัน (`ws.onclose`) กันไม่ให้ค้างโกรธข้าม session ไปหาคนถัดไป |
  | *(ไม่มี CatState ไหนแมปมา)* | `waking` | ยังไม่มีอะไรเรียกถึง — มี wake-word listener แบบ dev-only แล้ว (`useWakeWord.ts`) แต่ยังไม่ได้ต่อเข้า `waking` render-state และโมเดลยังไม่ผ่าน production gate ดูหัวข้อ "ฟีเจอร์ในสโคปที่ยังไม่ได้ทำ" |

  **`angry` ค้าง/หายกลางคัน — แก้แล้ว (2026-09-08):** เดิมสงสัยว่า error "1008 policy violation:
  The operation was aborted" ที่เจอตอนทดสอบเป็นอาการของเสียงสังเคราะห์ที่ใช้ทดสอบเอง — **ผิด** ยืนยัน
  จากเอกสารจริงแล้วว่าเป็น Gemini Live audio-only session duration limit จริง (15 นาที ส่ง `GoAway`
  ล่วงหน้า ~60s ดู https://ai.google.dev/gemini-api/docs/live-session) แก้ด้วย session resumption
  reconnect ฝั่ง backend (ดูคอมเมนต์ `MAX_CONSECUTIVE_GEMINI_RECONNECT_FAILURES` ใน `routers/voice.py`)
  ส่วนอาการ "หน้าโกรธแว้บหายกลางเทิร์น" (คนละเรื่องกับข้างบน) root cause คือ `tick()` (60fps) เห็น
  ช่องว่างจริง ~7-90ms ระหว่างก้อนเสียงที่ต่อกัน (`scheduleChunk`) เข้าใจผิดว่าจบเทิร์นทั้งที่ยังพูดต่อ
  — แก้ด้วย `BOT_SPEECH_END_HANGOVER_MS` (`useVoiceSocket.ts`) ยืนยันแล้วด้วย log จริงจากผู้ใช้ว่าไม่
  หลุดอีก

  **เสียงติ๊ก/ป็อบแทรกระหว่างแมวพูด — ทดลองแก้แล้ว ยังไม่ยืนยันด้วยอุปกรณ์จริง (2026-09-08):** ผู้ใช้ได้ยินเสียงติ๊ดสม่ำเสมอตอนแมว
  ตอบ (เกิดถี่มาก บางรอบ "เยอะมากเลย") ตัดสาเหตุออกไปแล้ว 3 อย่าง:
  1. ไม่ใช่รอยต่อ chunk ที่ `scheduleChunk` ต่อกันตรง ๆ — เคยลอง fade เข้า-ออก 3ms ที่ต้น-ปลายแต่ละ
     ก้อนไปแล้ว (commit `48b4860`) **แต่ทำให้แย่ลง** เพราะ fade เองสร้างรอยกระโดดที่เดิมไม่มี (ก้อน
     PCM ต่อกันสนิทสมบูรณ์อยู่แล้ว — พิสูจน์จากไฟล์ที่อัดเสียงดิบจาก Gemini ก่อนส่งต่อ browser แล้วฟัง
     สะอาดดี) revert ไปแล้ว (`32f646c`) **ห้ามลองใส่ fade แบบนี้ซ้ำ**
  2. ไม่ใช่ sample rate mismatch — ตรวจ mime_type จริงจาก Gemini (`audio/pcm;rate=24000`) ตรงกับที่
     backend relay ตรง ๆ (ไม่ resample) และ `OUTPUT_SAMPLE_RATE` ฝั่ง frontend เป๊ะทั้ง 3 จุด
  3. ไม่ใช่ echo cancellation/AGC ของไมค์ (คนละ audio path กับ playback อยู่แล้ว ไม่น่าเกี่ยว)

  ตัวต้องสงสัยตอนนี้: `scheduleChunk` มี branch `if (nextPlayTimeRef.current < now)` ที่ตรวจพบ
  "คิวไล่ไม่ทัน" จริง จาก log จริงของผู้ใช้เจอทั้ง gap ใหญ่ (ปกติ ตอนเริ่มเทิร์นใหม่หลังเงียบ ไม่มีใครได้
  ยิน) และ gap เล็ก 10-70ms ที่เกิดซ้ำได้หลายครั้งในเทิร์นเดียว

  **รอบที่ 1 (2026-09-08 เช้า) — เดาผิด:** สงสัยว่ามาจาก loop flush jitter buffer ตอนเทิร์นเริ่ม
  (สร้าง `AudioBuffer` หลายก้อน synchronous จนตามหลัง real-time) เลยใส่ `CHUNK_RECOVERY_MARGIN_MS = 10`
  ที่จุดเดียวคือ `enqueueAudio` ตอน flush queue เท่านั้น — **ผิด** พิสูจน์ด้วยหลักฐานจริงในรอบที่ 2

  **รอบที่ 2 (2026-09-08 บ่าย) — ใส่ diagnostic logging เก็บหลักฐานจริง:** ผู้ใช้ทดสอบบน Mac (เบราว์
  เซอร์เดสก์ท็อป ไม่ใช่แท็บเล็ตจริง — ผลจึงยังไม่ครบตามแผนทดสอบ) เปิด Console เห็น
  `[click-noise-debug]` **11 ครั้งที่เจอเป็น `origin=stream` ล้วนๆ ไม่มี `origin=flush` แม้แต่ครั้ง
  เดียว** — แปลว่า margin รอบแรกที่ใส่ไปไม่มีผลอะไรเลยกับบั๊กจริง (ไม่ใช่แค่ "ครอบคลุมแค่เทิร์นแรก"
  แต่คือ "ไม่เคยถูกเรียกใช้เลยทั้ง session" เพราะ mechanism จริงอยู่คนละจุด) ขนาด gap เล็กที่เจอจริง
  (ตัดกลุ่มใหญ่ที่เป็นความเงียบระหว่างเทิร์นทิ้ง ไม่มีใครได้ยิน): **5.8ms, 40.6ms, 69.6ms, 75.4ms**
  ทั้งหมดเกิดตอน schedule ทีละก้อนที่มาจาก WS ระหว่างเทิร์น (`origin=stream`) ไม่ใช่ลูป flush ตอน
  เทิร์นเริ่มตามที่เดาไว้แต่แรก

  **รอบที่ 3 (2026-09-08 บ่าย, ตามคำสั่งผู้ใช้ "ขยาย fix"):** ย้าย `CHUNK_RECOVERY_MARGIN_MS = 10`
  จากจุดเดียว (`enqueueAudio` flush) มาใส่ใน `computeChunkSchedule` ตรงๆ (ใส่ทุกครั้งที่เกิด `hadGap`
  ไม่ว่า origin ไหน) — ครอบคลุมทั้ง `flush` และ `stream` แล้วด้วยกลไกเดียว ไม่ต้อง diagnose แยกอีกว่า
  origin ไหนสำคัญกว่ากัน diagnostic logging (`[click-noise-debug]`) **ยังอยู่ในโค้ด** (ตั้งใจไม่ลบ
  จนกว่าจะทดสอบซ้ำแล้วเห็นว่า gap ลดลง/หายไปจริง — ต้องลบก่อน commit เข้าทีมตามแนวทาง commit
  `3542c85` เดิม)

  ⚠️ **trade-off ที่ยืนยันด้วยแบบจำลองแล้ว (ดูเทสในไฟล์เดียวกัน):** ใส่ margin ไม่ได้ลดขนาด gap ที่
  กำลังเกิดครั้งนั้น (จริงๆ ทำให้ครั้งนั้นใหญ่ขึ้นอีก margin หนึ่ง) แต่ให้ lead time ก้อนถัดไปมีโอกาส
  ไม่ gap ซ้ำในช่วงสั้นๆ ถัดมา — แลก "ความถี่" กับ "ขนาดครั้งที่เกิด" ไม่รู้ว่าหูมนุษย์จะรู้สึกดีขึ้นหรือ
  แย่ลงจนกว่าจะฟังจริง เสี่ยงซ้ำรอยบั๊กเดิมที่เคยลอง fade แล้วแย่ลงกว่าเดิม (commit 48b4860/32f646c —
  คนละกลไกกับ fade ไม่ได้แตะการ fade เข้า-ออกเลย) มีเทสยืนยันตรรกะ (ไม่ใช่เสียงจริง) ที่
  `frontend-character/src/hooks/useVoiceSocket.test.ts`

  **⚠️ มิติ trade-off เพิ่มเติมที่ reviewer ชี้ให้เห็น (ยังไม่เคยพูดถึงมาก่อน):** margin ที่บวกเข้า
  `nextPlayTimeRef` ทุกครั้งที่ `hadGap` มีผลกับ `isBotSpeaking()` ด้วย (ใช้ตัดสิน half-duplex —
  ปิดไมค์ตอนแมวพูด) เพราะอิงค่าเดียวกัน แปลว่าไมค์จะปิดยาวขึ้นสะสมทุกเทิร์นที่มี gap (~จำนวน gap ×
  margin ต่อเทิร์น — จากหลักฐานจริงคือหลักสิบ ms ไม่ใช่หลักร้อย ไม่ถึงขั้นกระทบการสนทนา แต่เป็นมิติที่ต้อง
  รู้ไว้คู่กับ "ความถี่ vs ขนาด gap ที่ได้ยิน" ด้านบน)

  **⚠️ ยังไม่ยืนยันว่าหายจริงด้วยหูจริง** ต้องทดสอบซ้ำบนแท็บเล็ตจริงตาม
  `docs/click-noise-manual-test-plan.md` (เทสบน Mac ที่ทำไปแค่เก็บ diagnostic evidence เรื่อง
  origin ไม่ใช่การยืนยันว่าเสียงหายแล้ว) พร้อมเปิด Console ดู `[click-noise-debug]` เทียบก่อน-หลังว่า
  จำนวนครั้ง/ขนาดเปลี่ยนไปไหม — ถ้าทดสอบแล้วไม่ดีขึ้น/แย่ลง ให้ตั้ง `CHUNK_RECOVERY_MARGIN_MS = 0` เพื่อ
  revert กลับพฤติกรรมเดิมได้ทันที (ค่าคงที่จุดเดียว ไม่ต้องแก้โค้ดจุดอื่น) แล้วกลับมาคิดแนวทางใหม่
- ตอบเฉพาะขอบเขต CAMT, DITC และเนื้อหาบนเว็บ DITC
  คำถามนอกขอบเขตให้ปฏิเสธสุภาพแล้วชวนกลับเข้าหัวข้อ

## เรื่องที่ยังไม่ชัด ต้องถามผู้ใช้ ห้ามเดา

- TOR ข้อ 3.3 บอกให้เก็บประวัติการสนทนา แต่ Overview บอกว่าไม่เก็บ
  บทสนทนาเต็ม เก็บแค่หัวข้อสรุป — ยังไม่ได้เคลียร์ว่าเอาแบบไหน
- สเปกเครื่องแม่ข่ายที่จะติดตั้งจริง ยังไม่รู้ว่ารัน Docker + e5-large ไหวไหม

## เอกสารอ้างอิง

เอกสารสโคปจริงอยู่ที่ docs/scope/
- project-overview.pdf — ภาพรวมโปรเจค ฟีเจอร์ สถาปัตยกรรม (7 ส.ค. 2569)
- tor-45k.pdf — ร่างขอบเขตงานจ้าง เงื่อนไขสัญญา (3 ก.ค. 2569)

เมื่อไม่แน่ใจว่าฟีเจอร์ไหนอยู่ในสโคปหรือไม่ ให้อ่านสองไฟล์นี้ก่อน
อย่าเดา CLAUDE.md สรุปมาแล้วแต่ไม่ครบทุกรายละเอียด
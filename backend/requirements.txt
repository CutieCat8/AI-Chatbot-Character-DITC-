# ---- Web framework ----
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.13.5
pydantic-settings==2.7.1

# ---- Database ----
sqlalchemy==2.0.36
psycopg[binary]==3.2.3
pgvector==0.3.6
alembic==1.14.0

# ---- Auth (Sprint 4, T27) ----
python-jose[cryptography]==3.3.0
bcrypt==5.0.0

# ---- Scraper (T03) ----
beautifulsoup4==4.12.3   # แยกเนื้อหาออกจาก HTML
lxml==5.3.0              # parser เร็วให้ BeautifulSoup

# ---- Utilities ----
python-dotenv==1.0.1
httpx==0.28.1

# ---- Voice (Gemini Live) ----
google-genai==2.21.0

# ---- Voice pipeline (dev-machine mic/speaker + VAD gating) ----
sounddevice==0.5.6
silero-vad==6.2.1   # ใช้ torch ตัวเดียวกับ embedding ข้างล่าง ไม่โหลดโมเดลผ่านเน็ตซ้ำ (bundle มาในแพ็กเกจ)

# ---- Embedding (local, ฟรี — ดู docs/adr/embedding-model.md) ----
# CPU wheel เท่านั้น ไม่ต้องมี GPU/CUDA (เครื่อง dev/demo ไม่มี GPU)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.14.0
sentence-transformers==6.0.1

# ---- Dev / Test ----
pytest==8.3.4
